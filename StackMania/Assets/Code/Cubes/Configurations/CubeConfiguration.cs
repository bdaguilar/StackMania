public class CubeConfiguration
{
    public readonly float Speed;
    public readonly MoveDirection Direction;

    public CubeConfiguration(float speed, MoveDirection direction)
    {
        Speed = speed;
        Direction = direction;
    }
}